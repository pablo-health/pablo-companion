<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Font families — loaded from app-local fonts -->
    <FontFamily x:Key="PabloDisplayFont">ms-appx:///Assets/Fonts/Fraunces.ttf#Fraunces</FontFamily>
    <FontFamily x:Key="PabloBodyFont">ms-appx:///Assets/Fonts/DMSans.ttf#DM Sans 9pt</FontFamily>

    <!-- Display styles (Fraunces) -->
    <Style x:Key="PabloDisplayLarge" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PabloDisplayFont}" />
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource PabloBrownDeep}" />
    </Style>

    <Style x:Key="PabloDisplayMedium" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PabloDisplayFont}" />
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource PabloBrownDeep}" />
    </Style>

    <!-- Body styles (DM Sans) -->
    <Style x:Key="PabloBodyLarge" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PabloBodyFont}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Foreground" Value="{StaticResource PabloBrownDeep}" />
    </Style>

    <Style x:Key="PabloBodyMedium" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PabloBodyFont}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource PabloBrownDeep}" />
    </Style>

    <Style x:Key="PabloBodySmall" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PabloBodyFont}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource PabloBrownSoft}" />
    </Style>

</ResourceDictionary>
